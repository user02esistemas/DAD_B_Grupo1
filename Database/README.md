# Database

Scripts SQL del sistema EconoSalud.

Archivos:

```text
schema.sql    Crea la base de datos, tablas, relaciones, datos iniciales y objetos SQL necesarios.
```

Base de datos local:

```text
bd_BoticaEconoSalud
```

Importacion recomendada:

```text
1. Encender MySQL/MariaDB desde XAMPP.
2. Abrir phpMyAdmin o consola MySQL.
3. Importar Database/schema.sql.
```
